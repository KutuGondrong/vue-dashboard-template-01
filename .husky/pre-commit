#!/usr/bin/env sh
set -e

# 1) Prettier + ESLint on staged files only (lint-staged runs format first, then lint)
pnpm exec lint-staged

# 2) Typecheck whole project before commit
pnpm exec vue-tsc --noEmit
